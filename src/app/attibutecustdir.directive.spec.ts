import { AttibutecustdirDirective } from './attibutecustdir.directive';

describe('AttibutecustdirDirective', () => {
  it('should create an instance', () => {
    const directive = new AttibutecustdirDirective();
    expect(directive).toBeTruthy();
  });
});
